[build]
builder = "nixpacks"

[deploy]
startCommand = "python _activity_probe.py || true; streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
